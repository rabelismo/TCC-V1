[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
watchPatterns = ["src/**", "pom.xml", "Dockerfile", "docker-entrypoint.sh"]

[deploy]
healthcheckPath = "/api/modules"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
